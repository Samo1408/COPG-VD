<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.copgvd.xposed">

    <application
        android:allowBackup="false"
        android:label="COPG-VD Spoof"
        android:supportsRtl="true">

        <!-- LSPosed/Xposed module -->
        <meta-data android:name="xposedmodule" android:value="true" />
        <meta-data android:name="xposeddescription"
            android:value="COPG-VD Device Spoofing - Hooks system_server to spoof device identity for all apps. Zero footprint in target apps." />
        <meta-data android:name="xposedminversion" android:value="93" />
        <!-- Scope: System Framework only (hooks run in system_server, all apps benefit) -->
        <meta-data android:name="xposedscope" android:resource="@array/xposed_scope" />

    </application>
</manifest>
